import { useEffect } from "react";
import { useLocation, useNavigate } from "react-router-dom"
import { getToken } from "./utils/auth";

// before route
// to start progress
// to set page title
// to determine whether the user has logged in
// if the user is logged
  // if the current page is login page, redirect dashboard and end the progress
  // if not user info
    // to get user info
// user not logged
  // if the path is in whiteList, path to
  // 